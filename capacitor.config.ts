import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moh.safemama',
  appName: 'Safe Mama',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
